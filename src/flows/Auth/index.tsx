import React from 'react';
import {Layout} from './components/Layout';
import {Text} from 'react-native';

export const Auth = () => {
  return (
    <Layout>
      <Text>Auth</Text>
    </Layout>
  );
};
